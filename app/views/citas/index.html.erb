<p style="color: green"><%= notice %></p>

<h1>Citas</h1>

<div id="citas">
  <% @citas.each do |cita| %>
    <%= render cita %>
    <p>
      <%= link_to "Show this cita", cita %>
    </p>
  <% end %>
</div>

<%= link_to "New cita", new_cita_path %>
